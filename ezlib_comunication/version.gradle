allprojects {
    ext {
        versionMajor = 1
        versionMinor = 0
        versionPatch = 0
        nameApk = "Ezlib_Comunication"
        versionName = "${versionMajor}.${versionMinor}.${versionPatch}"
    }
}